# Configuración del Servidor
# Copiar este archivo a .env y ajustar los valores según necesidad

# Entorno de ejecución (development o production)
FLASK_ENV=development

# Host y Puerto
HOST=0.0.0.0
PORT=5000

# SSL/HTTPS
# Habilitar SSL (true o false)
SSL_ENABLED=false

# Rutas a certificados SSL (solo si SSL_ENABLED=true)
SSL_CERT_PATH=certs/cert.pem
SSL_KEY_PATH=certs/key.pem

# Clave secreta para Flask (CAMBIAR EN PRODUCCIÓN)
SECRET_KEY=change-this-secret-key-in-production

# CORS - Orígenes permitidos (separados por coma)
# Usar * para permitir todos (solo desarrollo)
CORS_ORIGINS=*

# Directorio de datos
DATA_DIR=data
